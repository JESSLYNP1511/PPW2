<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #fce4ec; /* Warna pink pastel */
            margin: 0;
        }
        .login-container {
            background: white;
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
            width: 320px;
            position: relative;
        }
        .login-container img {
            width: 80px;
            height: 80px;
            margin-bottom: 15px;
        }
        h2 {
            margin-bottom: 15px;
            color: #d63384; /* Warna pink */
        }
        input {
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            border: 1px solid #ff85a2;
            border-radius: 8px;
            background: #fff0f6;
        }
        input:focus {
            outline: none;
            border-color: #ff4081;
            background: #ffe0f0;
        }
        button {
            width: 100%;
            padding: 12px;
            background: #ff69b4;
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 16px;
            transition: 0.3s;
        }
        button:hover {
            background: #ff4081;
        }
        p {
            margin-top: 10px;
            font-size: 14px;
        }
        a {
            color: #d63384;
            text-decoration: none;
            font-weight: bold;
        }
        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <img src="https://cdn-icons-png.flaticon.com/512/742/742751.png" alt="Cat Icon">
        <h2>Welcome Meow! 🐱</h2>
        <form action="/auth/login" method="POST">
            <input type="text" name="username" placeholder="Username" required>
            <input type="password" name="password" placeholder="Password" required>
            <button type="submit">Login</button>
        </form>
        <p>Don't have an account? <a href="/auth/register">Register</a></p>
    </div>
</body>
</html>
